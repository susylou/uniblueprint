/**
 * au-hub-grid - client controller.
 * Cards are rendered as anchor tags with ng-href so navigation is native.
 * No client logic needed yet; placeholder controller for parity with the
 * other widgets in case we add interactions later (filter chip, search).
 */
function HubGridController() {
    // intentionally empty
}

api.controller = HubGridController;
